[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Le API route di Next.js (App Router) vengono automaticamente convertite
# da @netlify/plugin-nextjs in Netlify Functions: nessuna configurazione
# manuale aggiuntiva è necessaria per /app/api/parse-payslip.
